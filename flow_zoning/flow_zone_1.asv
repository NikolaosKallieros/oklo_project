clear;clc;close all;
%% create hexagonal pattern 
x_original = 1:1:10;
y_original = 1:1:10;
%%
x1 = [1 1.4 5 4 6 6.6];
figure 
hold on 
histogram(power_dist, 60)
for i=1:length(x1)
xline(x1(i),"--","color","r");
end
xlabel("power")
ylabel("discrete distribution")

power_dist1 = power_dist + 1 + (4-1).*rand(1,1). 